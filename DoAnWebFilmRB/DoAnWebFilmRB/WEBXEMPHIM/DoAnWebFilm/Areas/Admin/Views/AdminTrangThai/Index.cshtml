@model IEnumerable<DoAnWebFilm.Models.TRANGTHAI>

@{ ViewBag.Title = "Index";
Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml"; }

<h2 class="text-secondary text-center p-2">TRẠNG THÁI</h2>
<style>
    table {
        border-collapse: collapse;
        width: 100%;
        margin: auto;
    }

    th, td {
        text-align: center;
        padding: 8px;
    }

    tr:nth-child(even) {
        background-color: #f2f2f2
    }

    th {
        background-color: #0cafb4;
        color: white;
    }
</style>
<table>
    <tr>
        <th>ID Trạng Thái</th>
        <th>Tên Trạng Thái</th>
        <th colspan="2">
            <a href="@Url.Action("Create","AdminTrangThai")"><i class="fas fa-plus-circle text-warning fa-2x"></i></a>
        </th>
    </tr>
    @foreach (var item in Model)
    {

        <tr>
            <td>@item.ID_TRANGTHAI</td>
            <td>@item.TEN_TRANGTHAI</td>
            <td>
                <a href="@Url.Action("Edit","AdminTrangThai",new { id= @item.ID_TRANGTHAI})"><i class="fas fa-edit text-success fa-2x"></i></a>
            </td>
            <td>
                <a href="@Url.Action("Delete","AdminTrangThai",new { id= @item.ID_TRANGTHAI})"><i class="fas fa-trash-alt text-danger fa-2x"></i></a>
            </td>
        </tr>

    }
</table>
